{
  "name": "dynamodb-restore",
  "version": "0.0.1",
  "description": "Detect Dynamodb table delete and restore it from one of the available backups",
  "scripts": {
    "test": "nyc mocha",
    "deploy": "serverless deploy --stage dev",
    "remove": "serverless remove --stage dev"
  },
  "author": "Athar Khan",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "aws-sdk": "^2.814.0",
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "nock": "^11.3.5",
    "nyc": "^14.1.1",
    "proxyquire": "^2.1.3",
    "serverless-iam-roles-per-function": "^2.0.2"
  }
}